{
  "name": "ampel360-framework-generator",
  "version": "1.0.0",
  "description": "TypeScript framework structure generator for AMPEL360 H₂-BWB-Q",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test-suite": "npm run build && node dist/test-suite.js",
    "lint": "eslint src/**/*.ts",
    "start": "node dist/index.js",
    "generate": "npm run build && node dist/index.js > ampel360-framework.json 2>&1",
    "generate-json": "npm run build && node -e \"const {generateFrameworkStructure,frameworkToJSON}=require('./dist/lib');const fs=require('fs');const framework=generateFrameworkStructure('','','','','',[], {includeFolders:true,includeDescriptions:true});fs.writeFileSync('ampel360-framework-clean.json', frameworkToJSON(framework));\""
  },
  "keywords": [
    "ampel360",
    "framework",
    "typescript",
    "aviation",
    "bwb"
  ],
  "author": "AMPEL360 Team",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.40.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}